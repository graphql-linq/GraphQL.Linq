type Category {
  description: String
  id: Int!
  isActive: Boolean!
  name: String!
}

"A connection from an object to a list of objects of type 'Category'."
type CategoryConnection {
  "A list of all of the edges returned in the connection."
  edges: [CategoryEdge!]!
  "A list of all of the objects returned in the connection. This is a convenience field provided for quickly exploring the API; rather than querying for \"{ edges { node } }\" when no edge data is needed, this field can be used instead. Note that when clients like Relay need to fetch the \"cursor\" field on the edge to enable efficient pagination, this shortcut cannot be used, and the full \"{ edges { node } } \" version should be used instead."
  items: [Category!]!
  "Information to aid in pagination."
  pageInfo: PageInfo!
  "A count of the total number of objects in this connection, ignoring pagination. This allows a client to fetch the first five objects by passing \"5\" as the argument to `first`, then fetch the total count so it could display \"5 of 83\", for example. In cases where we employ infinite scrolling or don't have an exact count of entries, this field will return `null`."
  totalCount: Int!
}

"An edge in a connection from an object to another object of type 'Category'."
type CategoryEdge {
  "A cursor for use in pagination"
  cursor: String!
  "The item at the end of the edge"
  node: Category!
}

scalar Decimal

type OrderItem {
  orderId: Int!
  productId: Int!
  quantity: Int!
  unitPrice: Decimal!
}

"A connection from an object to a list of objects of type 'OrderItem'."
type OrderItemConnection {
  "A list of all of the edges returned in the connection."
  edges: [OrderItemEdge!]!
  "A list of all of the objects returned in the connection. This is a convenience field provided for quickly exploring the API; rather than querying for \"{ edges { node } }\" when no edge data is needed, this field can be used instead. Note that when clients like Relay need to fetch the \"cursor\" field on the edge to enable efficient pagination, this shortcut cannot be used, and the full \"{ edges { node } } \" version should be used instead."
  items: [OrderItem!]!
  "Information to aid in pagination."
  pageInfo: PageInfo!
  "A count of the total number of objects in this connection, ignoring pagination. This allows a client to fetch the first five objects by passing \"5\" as the argument to `first`, then fetch the total count so it could display \"5 of 83\", for example. In cases where we employ infinite scrolling or don't have an exact count of entries, this field will return `null`."
  totalCount: Int!
}

"An edge in a connection from an object to another object of type 'OrderItem'."
type OrderItemEdge {
  "A cursor for use in pagination"
  cursor: String!
  "The item at the end of the edge"
  node: OrderItem!
}

"Information about pagination in a connection."
type PageInfo {
  "When paginating forwards, the cursor to continue."
  endCursor: String
  "When paginating forwards, are there more items?"
  hasNextPage: Boolean!
  "When paginating backwards, are there more items?"
  hasPreviousPage: Boolean!
  "When paginating backwards, the cursor to continue."
  startCursor: String
}

type Product {
  categories: [ProductCategory!]!
  categoriesConnection(
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): ProductCategoryConnection!
  categoriesConnectionProjected(
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): CategoryConnection!
  categoriesFromDbWithResolver(
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): ProductCategoryConnection!
  categoriesFromProductCategories: [Category!]!
  categoriesWithResolver(
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): ProductCategoryConnection!
  categoriesWithResolverNoName(
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): OrderItemConnection!
  categoriesWithType: [ProductCategory]!
  createdBy: User!
  createdByLink: User!
  createdByUser: User!
  createdByUserName: String!
  creator: User!
  creatorWithType: User
  delayLoadedCategories: [ProductCategory]!
  delayLoadedCategoriesProjected: [Category]!
  delayLoadedCreator: User!
  description: String
  dynamicField(includeDescription: Boolean): String
  filteredCategories(activeOnly: Boolean): [ProductCategory!]!
  filteredCategoriesConnection(
    activeOnly: Boolean,
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): ProductCategoryConnection!
  filteredCategoriesWithResolver(
    activeOnly: Boolean,
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): ProductCategoryConnection!
  id: ID!
  isActive: Boolean!
  name: String!
  nonNullableCreator: User!
  nonNullableDescription: String!
  nullableCreator: User
  nullableDescription: String
  nullableId: ID
  orderItems(
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): OrderItemConnection!
  price: Decimal!
  priceAsFloat: Float
  productCategories: [ProductCategory!]!
  productCategoriesConnection(
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): ProductCategoryConnection!
  productCategoriesFromDb: [ProductCategory!]!
  productCategoriesFromDbConnection(
    "Only look at connected edges with cursors greater than the value of `after`."
    after: String,
    "Only look at connected edges with cursors smaller than the value of `before`."
    before: String,
    "Specifies the number of edges to return starting from `after` or the first entry if `after` is not specified."
    first: Int,
    "Specifies the number of edges to return counting reversely from `before`, or the last entry if `before` is not specified."
    last: Int): ProductCategoryConnection!
  productId: ID!
  productName: String!
  stockQuantity: Int!
}

type ProductCategory {
  category: Category!
  categoryId: Int!
  product: Product!
  productId: Int!
}

"A connection from an object to a list of objects of type 'ProductCategory'."
type ProductCategoryConnection {
  "A list of all of the edges returned in the connection."
  edges: [ProductCategoryEdge!]!
  "A list of all of the objects returned in the connection. This is a convenience field provided for quickly exploring the API; rather than querying for \"{ edges { node } }\" when no edge data is needed, this field can be used instead. Note that when clients like Relay need to fetch the \"cursor\" field on the edge to enable efficient pagination, this shortcut cannot be used, and the full \"{ edges { node } } \" version should be used instead."
  items: [ProductCategory!]!
  "Information to aid in pagination."
  pageInfo: PageInfo!
  "A count of the total number of objects in this connection, ignoring pagination. This allows a client to fetch the first five objects by passing \"5\" as the argument to `first`, then fetch the total count so it could display \"5 of 83\", for example. In cases where we employ infinite scrolling or don't have an exact count of entries, this field will return `null`."
  totalCount: Int!
}

"An edge in a connection from an object to another object of type 'ProductCategory'."
type ProductCategoryEdge {
  "A cursor for use in pagination"
  cursor: String!
  "The item at the end of the edge"
  node: ProductCategory!
}

type Query {
  product(id: ID!): Product!
}

type User {
  email: String!
  id: Int!
  isActive: Boolean!
  username: String!
}
